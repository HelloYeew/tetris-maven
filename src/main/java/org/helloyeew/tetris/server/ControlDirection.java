package org.helloyeew.tetris.server;

/**
 * Enum for the direction of the control from player to make the server know the direction to broadcast the event.
 */
public enum ControlDirection {
    LEFT,
    RIGHT,
    UP
}
